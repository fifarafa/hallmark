{
  "name": "hallmark-run",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic workflow proof-of-concept for agentic software delivery (spec -> plan -> build -> review -> ship).",
  "scripts": {
    "hallmark": "tsx src/cli.ts",
    "test": "node --import tsx --test test/*.test.ts",
    "university:test": "node --import tsx --test example-university/bank/reference/test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "^0.12.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
